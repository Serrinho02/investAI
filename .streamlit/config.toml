[client]
toolbarMode = "viewer"  # Nasconde la toolbar sviluppatore (rerun, settings)

[theme]
base = "light"          # Forza il tema scuro (opzionale, ma professionale)
primaryColor = "#31d3f7"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"

[browser]
gatherUsageStats = false # Disabilita invio statistiche
showErrorDetails = false    # Se c'è un errore, mostra solo "Error" senza codice tecnico

[ui]
hideTopBar = true           # Nasconde la barra colorata superiore (funziona solo in alcuni casi)
